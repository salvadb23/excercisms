function find_middle(ll){
    middle = ll.count // 2
    count = 0
    current_node = ll.head
    while (current_node != Null){
        if (counter= mid_num){
            break}
        current_node = current_node.next
        counter += 1
    }

    return (current_node.data, current_node.next.data)
}